# Dependencies
node_modules/
node_modules

# Build output
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
coverage/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
# Root config declaration artifacts emitted by `tsc -b`
/vite.config.d.ts
/vite.config.node.d.ts

# Server build output
dist-server/

# ATLAS framework data (repo-root only — must NOT match the Splunk app's
# default/data/ui views + nav, which are real source that ucc-gen does not generate).
/data/
memory/logs/
.tmp/

# Agent loop trace sink (JSONL)
.ucc-agent/

# Built SPA bundle staged into the native Splunk app — a build artifact produced by
# splunk-app/deploy/build_ui.sh (run automatically by build_agent_app.sh at package time).
# Not committed: keeps the 3.8 MB minified blob out of git and prevents source/bundle drift.
splunk-app/ucc-app/appserver/static/ui/app.js
splunk-app/ucc-app/appserver/static/ui/app.css

# Python bytecode (never commit / package)
__pycache__/
*.pyc
