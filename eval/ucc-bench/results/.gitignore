# Bench transcripts. Track only curated evidence + the latest pointers; ignore the
# per-run timestamped files and logs (they accumulate on every run).
*-2026-*.json
*-2027-*.json
run.log

# Keep curated, named evidence and the full-run latest pointer:
!simple-rest-poll.CLEAN.json
!full-5task.latest.json
!latest-full.json
# latest-dry-run.json is regenerated by --dry-run on every CI run; not tracked.
latest-dry-run.json
