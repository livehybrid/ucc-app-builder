# UI add-on: ucc-gen requires splunktaucclib (>=8.2.0 for Splunk 10 / ucc-gen 6.5).
splunktaucclib>=8.2.0
# Splunk Agent SDK engine deps (PyPI). NOTE: splunk-sdk 3.0.0 (which provides
# splunklib.ai) is NOT on public PyPI yet, so splunklib is vendored under lib/
# splunklib/ (Apache-2.0, from the splunk-sdk 3.0.0 wheel). py3.13 required.
langchain>=1.2.15
langchain-openai>=1.1.13
langgraph>=1.1.1
pydantic>=2.13.2
uuid-utils>=0.14.1
# CAP AT <2: the vendored Splunk Agent SDK's ToolRegistry (splunklib/ai/registry.py)
# registers handlers via the mcp 1.x decorators `Server.list_tools()` / `Server.call_tool()`,
# which mcp 2.0 REMOVED. On 2.x, ToolRegistry() raises AttributeError at construction, so
# bin/tools.py dies the moment the SDK spawns it and the agent reports the (very indirect)
# "MCPError: Connection closed" from the client's session.initialize(). Bit us 2026-07-29.
mcp>=1.27.0,<2
